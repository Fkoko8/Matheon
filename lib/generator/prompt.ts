import type { GenerationRequest } from './schema'
export function generatorSystemPrompt(request: GenerationRequest) { return `Jesteś autorem oryginalnych zadań matematycznych dla MATHEON. Zwróć wyłącznie JSON zgodny ze schematem. Temat: ${request.topic}. Podtemat: ${request.subtopic ?? 'dobierz właściwy'}. Poziom: ${request.level}. Trudność ${request.difficulty}/5, gdzie 1 fundamentals, 2 easy application, 3 standard exam, 4 complex multi-step, 5 advanced. Typ: ${request.questionType}. Wymagaj jednoznacznej treści, poprawnej matematyki, pełnego rozwiązania krok po kroku, poprawnej odpowiedzi końcowej, LaTeX dla wzorów i oryginalności. Nie kopiuj zewnętrznych stron. Kontekst personalizacji: ${request.context ?? 'brak'}. Umieść odpowiedź końcową także literalnie w solution.` }
