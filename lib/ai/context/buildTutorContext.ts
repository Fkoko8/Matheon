import { createServerSupabaseClient } from '@/lib/supabase/server'
import { searchKnowledge, type KnowledgeResult } from '@/lib/ai/retrieval/searchKnowledge'
export type TutorActivity = { lesson?: string; question?: string; answer?: string; attempts?: number; hintsUsed?: number }
export async function buildTutorContext(input: { query: string; activity?: TutorActivity; level?: string }) { const supabase = await createServerSupabaseClient(); const { data: { user } } = await supabase.auth.getUser(); const knowledge = await searchKnowledge(input.query, { level: input.level, limit: 6 }); let profile: Record<string, unknown> | null = null; let progress: Record<string, unknown>[] = []; let mistakes: Record<string, unknown>[] = []; if (user) { const [p, pr, m] = await Promise.all([supabase.from('profiles').select('level,xp,streak').eq('id', user.id).maybeSingle(), supabase.from('user_progress').select('topic_id,mastery,accuracy').eq('user_id', user.id).order('mastery').limit(8), supabase.from('mistakes').select('question_id,topic_id,mistake_type,attempt_count').eq('user_id', user.id).order('last_seen_at', { ascending: false }).limit(6)]); profile = p.data; progress = pr.data ?? []; mistakes = m.data ?? [] } return { knowledge, user: { profile, weakTopics: progress, mistakes }, activity: input.activity ?? {}, sourceAware: knowledge.length > 0 } }
export function formatTutorContext(context: Awaited<ReturnType<typeof buildTutorContext>>) { return JSON.stringify({ knowledge: context.knowledge.map((item: KnowledgeResult) => ({ title: item.title, content: item.content, source: item.metadata })), user: context.user, activity: context.activity, sourceAware: context.sourceAware }, null, 2) }
