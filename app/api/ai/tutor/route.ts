import { gateway, streamText } from 'ai'
import { createServerSupabaseClient } from '@/lib/supabase/server'
import { tutorSystemPrompt, tutorModeFromInput, type TutorContext, type TutorMode } from '@/lib/ai/tutor-prompt'
export const maxDuration = 60
export async function POST(request: Request) { const supabase = await createServerSupabaseClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return new Response('Unauthorized', { status: 401 }); const body = await request.json() as { messages?: { role: 'user' | 'assistant'; content: string }[]; context?: TutorContext; mode?: TutorMode; conversationId?: string }; const messages = (body.messages ?? []).slice(-12); const last = messages.at(-1); if (!last?.content?.trim()) return new Response('Message required', { status: 400 }); const mode = body.mode ?? tutorModeFromInput(last.content); const startedAt = Date.now(); let conversationId = body.conversationId; if (!conversationId) { const { data } = await supabase.from('ai_conversations').insert({ user_id: user.id, title: last.content.slice(0, 60) }).select('id').single(); conversationId = data?.id } if (conversationId) await supabase.from('ai_messages').insert({ conversation_id: conversationId, role: 'user', content: last.content, mode, context: body.context ?? {} }); const result = streamText({ model: gateway('openai/gpt-4.1-mini'), system: tutorSystemPrompt(mode, body.context ?? {}), messages, onFinish: async ({ text }) => { if (!conversationId) return; await supabase.from('ai_messages').insert({ conversation_id: conversationId, role: 'assistant', content: text, mode, response_ms: Date.now() - startedAt, context: body.context ?? {} }); await supabase.from('ai_conversations').update({ updated_at: new Date().toISOString() }).eq('id', conversationId) } }); const response = result.toTextStreamResponse(); response.headers.set('x-conversation-id', conversationId ?? ''); return response }
