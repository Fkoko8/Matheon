import { createServerSupabaseClient } from '@/lib/supabase/server'
async function auth() { const supabase = await createServerSupabaseClient(); const { data: { user } } = await supabase.auth.getUser(); return { supabase, user } }
export async function GET() { const { supabase, user } = await auth(); if (!user) return new Response('Unauthorized', { status: 401 }); const { data: conversation } = await supabase.from('ai_conversations').select('id,title').eq('user_id', user.id).order('updated_at', { ascending: false }).limit(1).maybeSingle(); if (!conversation) return Response.json({ conversation: null, messages: [] }); const { data: messages } = await supabase.from('ai_messages').select('id,role,content,helpful').eq('conversation_id', conversation.id).order('created_at', { ascending: true }).limit(40); return Response.json({ conversation, messages: messages ?? [] }) }
export async function PATCH(request: Request) { const { supabase, user } = await auth(); if (!user) return new Response('Unauthorized', { status: 401 }); const { id, title } = await request.json(); const { error } = await supabase.from('ai_conversations').update({ title }).eq('id', id).eq('user_id', user.id); return error ? Response.json({ error: error.message }, { status: 500 }) : Response.json({ ok: true }) }
export async function DELETE(request: Request) { const { supabase, user } = await auth(); if (!user) return new Response('Unauthorized', { status: 401 }); const { id } = await request.json(); const { error } = await supabase.from('ai_conversations').delete().eq('id', id).eq('user_id', user.id); return error ? Response.json({ error: error.message }, { status: 500 }) : Response.json({ ok: true }) }
