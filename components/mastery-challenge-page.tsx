'use client'
import { useState } from 'react'
import Link from 'next/link'
import { ArrowLeft, CheckCircle2, ChevronRight, Target } from 'lucide-react'
import { getTopic } from '@/lib/learning/curriculum'
export function MasteryChallengePage({ slug }: { slug: string }) { const topic = getTopic(slug); const [selected, setSelected] = useState<string | null>(null); const [done, setDone] = useState(false); if (!topic) return <main className="p-10 text-white">Nie znaleziono tematu.</main>; return <main className="matheon-enter mx-auto max-w-3xl p-5 pb-28 lg:p-10"><Link href={`/learn/topic/${slug}`} className="inline-flex items-center gap-2 text-xs text-slate-500 hover:text-white"><ArrowLeft size={14} /> {topic.title}</Link><div className="mt-8 rounded-3xl border border-violet-400/20 bg-violet-500/10 p-6 md:p-8"><Target className="text-violet-300" size={22} /><p className="mt-5 text-[10px] font-semibold uppercase tracking-[0.2em] text-violet-300">Evidence-based mastery</p><h1 className="mt-2 text-3xl font-semibold text-white">Test opanowania: {topic.title}</h1><p className="mt-3 text-sm leading-6 text-slate-300">5 pytań sprawdzi definicje, procedury, zastosowanie i typowe pułapki. Otwarcie materiału nie podnosi wyniku mastery.</p></div><section className="mt-6 rounded-2xl border border-white/[0.07] bg-white/[0.025] p-6"><div className="flex items-center justify-between"><p className="text-sm font-semibold text-white">Pytanie 1 / 5</p><span className="text-xs text-slate-500">Poziom 3/5</span></div><h2 className="mt-6 text-lg font-medium leading-7 text-white">Który krok najlepiej rozpoczyna rozwiązanie zadania z działu „{topic.title}”?</h2><div className="mt-6 flex flex-col gap-2">{['Sprawdzenie danych i warunków zadania','Wstawienie liczb do pierwszego wzoru','Zapisanie samego wyniku','Pominięcie dziedziny'].map((option) => <button key={option} onClick={() => setSelected(option)} className={`flex items-center justify-between rounded-xl border p-4 text-left text-sm transition ${selected === option ? 'border-violet-300 bg-violet-500/15 text-white' : 'border-white/[0.08] text-slate-400 hover:border-violet-400/30 hover:text-white'}`}>{option}{selected === option && <CheckCircle2 size={16} className="text-violet-300" />}</button>)}</div>{done && <div className="mt-5 rounded-xl border border-emerald-400/20 bg-emerald-400/10 p-4 text-sm text-emerald-100">Wynik zapisany. Najmocniejsza jest analiza warunków; kolejną rekomendacją będzie trening zastosowań.</div>}<button disabled={!selected || done} onClick={() => setDone(true)} className="mt-6 flex items-center gap-2 rounded-xl bg-violet-500 px-4 py-3 text-xs font-medium text-white disabled:cursor-not-allowed disabled:opacity-40">Zapisz odpowiedź <ChevronRight size={14} /></button></section></main> }
